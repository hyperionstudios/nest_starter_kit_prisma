// import prisma client and the model you want to seed
// create prisma client
async function main() {
    // execute the seed
}

main()
    .catch((e) => {
        // display any errors
    })
    .finally(async () => {
        // Close connection to db
    });
